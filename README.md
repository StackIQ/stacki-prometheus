Adding the stacki-prometheus for monitoring.

By default, stacki-prometheus with no stacki-docker
or stacki-kubernetes pallet will create a dashboard
and prometheus instance on the frontend for monitoring
physical machines.
